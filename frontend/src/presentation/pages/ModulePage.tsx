import { ArrowUpRight, Plus, Search } from "lucide-react";

export default function ModulePage({ module }: { module: string }) {
  return <div className="mx-auto max-w-7xl"><div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8"><p className="text-sm font-bold uppercase tracking-[0.16em] text-teal-700">SegHIS workspace</p><h2 className="mt-2 text-3xl font-bold tracking-tight text-slate-900">{module}</h2><p className="mt-3 max-w-xl leading-7 text-slate-500">This area is ready for your connected data. As records are added, they’ll appear here with powerful filtering and quick actions.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" size={18} /><input className="w-full rounded-xl border border-slate-200 py-3 pl-10 pr-4 text-sm outline-none focus:border-teal-500" placeholder={`Search ${module.toLowerCase()}`} /></div><button className="inline-flex items-center justify-center gap-2 rounded-xl bg-slate-900 px-4 py-3 text-sm font-bold text-white"><Plus size={17} />Add new</button></div></div><div className="mt-6 grid gap-4 md:grid-cols-3">{["Recent activity", "Quick actions", "Need attention"].map((item, index) => <div key={item} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><p className="font-bold text-slate-800">{item}</p><ArrowUpRight size={18} className="text-slate-400" /></div><p className="mt-8 text-3xl font-bold text-slate-900">{index === 0 ? "—" : "0"}</p><p className="mt-1 text-sm text-slate-500">No items to display yet</p></div>)}</div></div>;
}
